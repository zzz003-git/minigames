-- 워커가 멈춘 것을 운영자에게 알렸는지 기억하는 칸 (2026-08-13)
--
-- 값이 있으면 「멈췄다고 이미 알렸다」는 뜻이고, 그 값 자체는 **마지막 생존 신고
-- 시각**이다. 한 칸이 두 가지를 겸하는 이유는 복구 알림 때문이다 — 되살아났을 때
-- 「얼마나 멈춰 있었나」를 말하려면 멈춘 시각이 필요한데, 그것이 곧 마지막 heartbeat 다.
--
-- NULL 로 되돌리는 것이 복구 처리이므로 상태가 둘뿐이고, **같은 장애로 10분마다
-- 알림이 반복되는 일이 구조적으로 생기지 않는다.** 반복해서 울리는 알림은 사람이
-- 꺼 버리고, 꺼진 알림은 없는 것과 같다.
ALTER TABLE ys_worker ADD COLUMN alert_down_at INTEGER;
