# 2026-08-24 · `docs/CHANGELOG.md` 를 `NOT_GAME_DOCS` 에 넣어 주십시오 — 조사 세션 요청서

**요청 대상**: `reward-minigame-research/scripts/sync_deployed_games.py`
**왜 직접 안 고쳤는가**: 그 파일의 소유가 문서 다섯 곳에서 엇갈려 있고, **소유권 결정(가·나·다)이 Master 대기 중**입니다. 조사 세션은 2026-08-21 회신 22-C 에서 **결정 전까지 `scripts/` 를 고치지 않겠다**고 적었고 그대로 지킵니다.

## 무엇이 일어났나 (2026-08-24 10:0X 실행 결과)

```
[알림] 분류되지 않은 문서 1개 — 지금은 감시에서 빠져 있습니다
    - docs/CHANGELOG.md
    → 게임 문서면 GROUP_DOCS 에, 미니게임이 아니면 NOT_GAME_DOCS 에 넣으세요.
```

**이 알림은 정상 동작입니다.** 8/21 에 넣은 「분류되지 않은 문서 알림」이 **의도한 대로 새 문서를 잡아냈습니다.** 다만 지금 상태로 두면 매일 같은 줄이 뜨고, **늘 켜져 있는 경보는 경보가 아니게 됩니다** — 개발 세션이 CHANGELOG 기준 커밋 설계에서 지적한 것과 같은 실패 모드입니다.

## 요청

`docs/CHANGELOG.md` 를 **`NOT_GAME_DOCS`** 에 추가해 주십시오. 게임 문서가 아니라 자동 생성 변경 이력이므로 대조군 감시 대상이 아닙니다.

- 되돌리기: 그 항목을 지우면 다시 「분류 안 됨」으로 뜹니다
- 회귀 확인 제안: 넣은 뒤 `python3 scripts/sync_deployed_games.py` 를 돌려 알림 줄이 사라지는지 확인

## 함께 보고

- 오늘 대조군 점검 결과는 **일치**입니다 — 서비스 게임 폴더 30 / 아케이드 레지스트리 26 / 대조군 30행, 폴더 전용 4종(baseball·memory·stopwatch·typing)은 설계상 정상.
- 오늘 조사 세션은 `git` 을 **한 번도 실행하지 않았습니다.** 갈래 2 로 `log` 가 허용됐지만, `CLAUDE.md` 규칙이 **첫 `git log` 는 Master 화면 앞에서 한 번만** 돌리도록 정하고 있고 지금은 자동 실행(부재)이기 때문입니다. 그래서 `CHANGELOG.md` 머리글의 기준 커밋 대조도 **오늘은 하지 않았습니다.**
